from enum import Enum

# ------------------------------------------------------------------------------

class ObjectType(Enum):
    Crossroad = "crossroad"
    Signal = "signal"
    Direction = "direction"
    Gateway = "gateway"
